{
  "name": "@acmecorp/dog-ui",
  "private": true,
  "module": "index.js",
  "type": "module",
  "scripts": {
    "postinstall": "spago install",
    "make": "spago build --pure",
    "test": "spago test --pure",
    "lint": "spago build --pure --strict --pedantic-packages && bun run scripts/fmt.js --check",
    "lint:fix": "bun run scripts/fmt.js",
    "bundle:prod": "bun run scripts/bundle.ui.js --prod",
    "bundle:dev": "bun run scripts/bundle.ui.js --dev"
  },
  "devDependencies": {
    "bun-types": "1.1.4",
    "esbuild": "^0.23.0",
    "glob": "^11.0.0",
    "purs-backend-es": "^1.4.2",
    "purs-tidy": "^0.10.0",
    "tailwindcss": "^3.4.7",
    "typescript": "^5.0.0",
    "yaml": "^2.6.0"
  },
  "dependencies": { }
}
